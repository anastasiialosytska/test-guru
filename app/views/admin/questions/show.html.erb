<div class="container mt-3">
  <h2><%= @question.body %></h2>
</div>

<div class="container mt-3">
  <h3><%= t('.answers') %></h3>
</div>

<ol>
  <%= render @question.answers %>
</ol>

<p><%= link_to t('.new_answer'), new_admin_question_answer_path(@question), class: 'btn btn-outline-secondary mt-2 mb-2' %></p>

<p><%= link_to t('.back'), admin_test_path(@test), class: 'btn btn-outline-secondary mt-2' %></p>
