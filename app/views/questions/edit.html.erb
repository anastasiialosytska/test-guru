<h1><%= question_header('edit') %></h1>

<%= form_with model: @question do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
<% end %>
